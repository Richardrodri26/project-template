// Aqui vas a implementar una mutation usando el fetcher, reactQuery y devolverlo

// import { appFetcher } from "@/domain/api/api.config";
// import { useMutation } from "@tanstack/react-query";

// Ejemplo

/**
 * hook than returns a sigin mutation
 * @returns
 */
// export const useSignIn = () => {
//   const mutation = useMutation({
//     mutationFn: (data: SigninInput) => appFetcher<{ signin: TuQuery }>(ELdocumentQuery, { signinInput: data }),
//   });

//   return mutation;
// };
